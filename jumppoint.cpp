#include <iostream>
#include <math.h>
#include <list>

using namespace std;

int main(){
    list<int> reach
    int n,r,a,b;
    cin >> n >> r;
    int tree[n][2];
    for(int i=0; i<n; i++){
        cin >> tree[i][0] >> tree[i][1];
    }
    

    return 0;
}