#include <iostream>
using namespace std;

int main(){
    int L,N,i,j,count,ans=1,test1,test2;
    cin >> L >> N;
    int bridge[N][2];
    for(i=0; i<N; i++){
        cin >> bridge[i][0] >> bridge[i][1];
    }

    for(i=0; i<N; i++){
        count = 0;
        test1 = bridge[i][0]; test2 = bridge[i][1];
        for(j=0; j<N; j++){
            if(bridge[j][0]<)
        }

        
    }
    return 0;
}